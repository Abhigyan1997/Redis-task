# Database Scripts

This application uses **MongoDB** with **Mongoose** for all data operations.

However, as per assessment requirements, a sample SQL schema has been provided in `init.sql` to demonstrate how the same structure might look in a relational database system such as PostgreSQL or MySQL.

> **Note:** The SQL file is for documentation only and is not used in the actual application.
